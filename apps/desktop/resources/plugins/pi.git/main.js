/**
 * Git — a first-party PI-Desktop plugin.
 *
 * The work-panel view is the entire surface. Git execution stays host-owned
 * and is reached only through the public bridge channels and permissions.
 */
export function onLoad() {}
